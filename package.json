{
  "name": "chat-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun --cwd packages/server run dev",
    "dev:client": "bun --cwd packages/client run dev"
  }
}
